import styled, { css } from 'styled-components';
import Debug from 'debug';

const debug = Debug('Mr.Papper::UI::ChirdrenPages');

const ChildrenPage = styled.div`
    display: block;
    margin: 20px;
`;

export default ChildrenPage;
